# sriov.conf — SR-IOV configuration for RTX 600 Pro
# Source this file or use values in enable_vfs.sh

# Number of Virtual Functions to enable
NUM_VFS=4

# DRM card device (check with: ls /sys/class/drm/)
CARD=card0
