# /etc/modprobe.d/rtx600pro.conf
# Driver module parameters

# Force xe driver probe (uncomment if needed)
# options xe force_probe=*

# Enable GuC firmware (required for SR-IOV)
# options xe enable_guc=3
