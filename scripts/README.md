# Scripts

| Folder         | Purpose                                          |
|----------------|--------------------------------------------------|
| `setup/`       | Driver installation and initial system setup     |
| `diagnostics/` | GPU health checks, log collection, debug tools   |
| `firmware/`    | NIC firmware discovery and update (Flint/mstflint) |
| `networking/`  | NIC benchmarking and network diagnostics         |
| `sriov/`       | PF/VF enablement and SR-IOV management           |
